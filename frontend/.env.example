# Copy to frontend/.env and fill in. Vite only exposes VITE_-prefixed vars.

# Base URL of the booking API. Leave unset to build a standalone site that
# prices from bundled defaults and stores admin edits in the browser only —
# that is what the GitHub Pages build does.
# VITE_API_URL=http://localhost:4000
